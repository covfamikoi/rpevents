import { ScrollView } from "react-native";
import { Text } from "react-native-paper";

export default function CreateConference() {
  return (
    <ScrollView>
      <Text>Create conference!</Text>
    </ScrollView>
  );
}
